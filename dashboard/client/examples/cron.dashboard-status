# /etc/cron.d/dashboard-status
# Push host metrics every minute. The spool covers transient failures.
* * * * * root /usr/local/bin/dashboard-push status --collect-host --body "host metrics" --dedup-key "status-$(hostname)" >/dev/null 2>&1
